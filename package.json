{
  "name": "api-v4",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --compile --outfile server",
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}'"
  },
  "dependencies": {
    "@elysiajs/cors": "^0.7.1",
    "@elysiajs/jwt": "^0.7.0",
    "@elysiajs/swagger": "^0.7.3",
    "elysia": "latest",
    "http-status-codes": "^2.3.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.0.3"
  },
  "module": "src/server.ts"
}
