{
  "name": "imps-api",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build --target=bun ./src/server.ts --outfile server.js",
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}'",
    "docker": "docker compose up -d --force-recreate --build"
  },
  "dependencies": {
    "@elysiajs/cors": "^0.7.1",
    "@elysiajs/swagger": "^0.7.3",
    "@sinclair/typebox": "^0.31.17",
    "elysia": "^0.7.15",
    "http-status-codes": "^2.3.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.0.3"
  },
  "overrides": {
    "cookie-signature": "latest"
  },
  "module": "src/server.ts"
}
